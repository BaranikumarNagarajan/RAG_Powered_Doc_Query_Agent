"""Server package init. Keeps the server directory importable as a package.

This file is intentionally empty; its presence ensures `import server` works
consistently across environments and invocation styles.
"""
